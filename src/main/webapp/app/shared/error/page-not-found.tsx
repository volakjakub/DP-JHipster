import React from 'react';

import { Alert } from 'reactstrap';

const PageNotFound = () => {
  return (
    <div>
      <Alert color="danger">Stránka neexistuje.</Alert>
    </div>
  );
};

export default PageNotFound;
